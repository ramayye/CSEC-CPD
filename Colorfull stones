s = input().strip()
t = input().strip()
position = 0  # 0-based index for easier handling

for instruction in t:
    if position < len(s) and s[position] == instruction:
        position += 1

print(position + 1)  # Convert to 1-based position

def min_shovels(k, r):
    for n in range(1, 11):  # Maximum 10 tries (as k * 10 is always valid)
        cost = n * k
        if cost % 10 == 0 or cost % 10 == r:
            return n

# Input reading
k, r = map(int, input().split())

# Output result
print(min_shovels(k, r))
